# Quick & Dirty role to install the default Tower file systems on Linux

Update the inventory/hosts file with your Tower and Tower DB nodes

Update inventory/group_vars and verify the device size, mapping, etc
